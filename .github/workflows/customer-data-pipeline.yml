# Auto-generated by the Repo Standardization Toolkit - Jenkins -> GitHub Actions migration.
# Source: Jenkinsfile in 'customer-data-pipeline'. Review every TODO before merging.
# Generated file - re-run migrate_jenkins.py to regenerate after Jenkinsfile changes.

name: customer-data-pipeline CI (migrated from Jenkins)
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch: {}
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.12
      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: "${{ secrets.AWS_PROD_ROLE_ARN }}   # TODO: confirm exact secret/variable name"
          aws-region: us-east-1
      - name: Lint
        run: flake8 src/
      - name: Unit Tests
        run: pytest --junitxml=report.xml
      - name: Terraform Plan
        run: terraform -chdir=infra plan -out=tfplan
      - name: Deploy to Dev
        run: terraform -chdir=infra apply tfplan
      - name: Approval (manual approval gate)
        run: "echo 'TODO: Jenkins used input{} here for a manual approval gate. Configure a GitHub Environment with required reviewers on this job instead.'"
      - name: Deploy to Prod
        run: terraform -chdir=infra apply -var env=prod tfplan
