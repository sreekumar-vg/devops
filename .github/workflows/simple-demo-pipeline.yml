# Auto-generated by the Repo Standardization Toolkit - Jenkins -> GitHub Actions migration.
# Source: Jenkinsfile in 'simple-demo-pipeline'. Review every TODO before merging.
# Generated file - re-run migrate_jenkins.py to regenerate after Jenkinsfile changes.

name: simple-demo-pipeline CI (migrated from Jenkins)
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch: {}
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.12
      - name: Checkout
        run: echo "Using inline demo files for this pipeline - no external repo checkout needed."
      - name: Build
        run: |
          mkdir -p build
          echo "Building demo artifact..."
          echo "demo-build-output" > build/artifact.txt
      - name: Test
        run: |
          test -f build/artifact.txt
          echo "Artifact found - test passed."
